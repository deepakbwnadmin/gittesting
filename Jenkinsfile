pipeline {



agent any
    // Build stages:
    stages {
			stage('Copy contents') {
            steps {
                sh "cp -r /root/pipelinetest /tmp/jenkins"
            		}
    		}
	}
}
